# 截图工具计划书

## 一、项目背景

用户需要一个简洁高效的截图小工具，要求打包成单个 EXE 文件，具备框选截图、自动保存、剪切板复制、颜色拾取等功能。

## 二、功能需求

### 2.1 核心功能
1. **框选截图**：点击截图按钮后隐藏界面，允许用户框选屏幕区域进行截图
2. **程序界面**：
   - 固定大小：屏幕的 1/10 宽高
   - 位置：右上角，距离上下边缘 1/6 处
   - 始终置顶
3. **界面元素**：
   - 截图按钮
   - 自动保存勾选框
   - 状态提示文本框
4. **截图后行为**：
   - 勾选自动保存：保存到当前文件夹 + 复制到剪切板
   - 不勾选：仅复制到剪切板
5. **截图时功能**：
   - 实时显示鼠标位置坐标
   - 实时显示当前坐标颜色值（16 进制格式如 #RRGGBB）
   - 鼠标上方显示 10x10 像素放大器
6. **取消操作**：按 ESC 或鼠标右键取消截图

### 2.2 技术选型
- **开发语言**：Python
- **GUI 框架**：`tkinter`（内置，打包体积小）或 `PyQt5/PySide6`（功能更强）
- **截图库**：`mss`（速度快）或 `PIL.ImageGrab`
- **打包工具**：`PyInstaller`（单文件模式）
- **颜色获取**：`PIL` 或 `pyautogui`

### 2.3 文件命名
- 格式：`screenshot_001.png`, `screenshot_002.png`（序号递增）

## 三、实现方案

### 3.1 项目结构
```
screenshot_tool/
├── main.py              # 主程序入口
├── screenshot_app.py    # 主界面类
├── capture_overlay.py   # 框选覆盖层类
├── utils.py             # 工具函数（保存、复制等）
├── config.py            # 配置管理
└── requirements.txt     # 依赖列表
```

### 3.2 核心模块设计

#### 3.2.1 主界面 (`screenshot_app.py`)
- 使用 tkinter 创建固定大小窗口
- 计算屏幕位置（右上角，距边缘 1/6）
- 设置窗口置顶
- 包含：截图按钮、自动保存复选框、状态文本框

#### 3.2.2 框选覆盖层 (`capture_overlay.py`)
- 全屏透明覆盖层
- 显示十字光标
- 实时显示坐标和颜色值
- 显示 10x10 放大器
- 处理鼠标事件（框选、取消）

#### 3.2.3 工具函数 (`utils.py`)
- 序号管理（读取/写入配置文件）
- PNG 保存
- 剪切板复制图片
- 颜色值格式化

### 3.3 关键技术点

1. **屏幕坐标计算**：
   ```python
   screen_width = root.winfo_screenwidth()
   screen_height = root.winfo_screenheight()
   win_width = screen_width // 10
   win_height = screen_height // 10
   win_x = screen_width - win_width - 50  # 距右边一定距离
   win_y = screen_height // 6
   ```

2. **框选截图流程**：
   - 隐藏主界面
   - 创建全屏透明覆盖层
   - 监听鼠标按下、移动、释放
   - 绘制选区矩形
   - 截图并裁剪

3. **颜色拾取**：
   - 使用 `mss` 截取单像素颜色
   - 或使用 `PIL.ImageGrab.grab(bbox=(x,y,x+1,y+1))`

4. **放大镜效果**：
   - 截取鼠标周围区域
   - 放大显示在tooltip 或独立小窗口

5. **打包配置**：
   ```bash
   pyinstaller --onefile --windowed --icon=icon.ico main.py
   ```

## 四、依赖列表

```txt
mss>=9.0.1
Pillow>=10.0.0
pyperclip>=1.8.0
pyinstaller>=6.0.0
```

## 五、开发步骤

1. **阶段一**：创建基础界面
   - tkinter 窗口
   - 位置计算
   - 基本控件

2. **阶段二**：实现截图功能
   - 覆盖层创建
   - 框选逻辑
   - 截图裁剪

3. **阶段三**：增强功能
   - 坐标和颜色显示
   - 放大镜
   - 自动保存和剪切板

4. **阶段四**：测试与打包
   - 功能测试
   - PyInstaller 打包
   - 体积优化

## 六、预期效果

- 启动快速（2 秒内）
- EXE 大小：15-30MB（使用 PyInstaller 单文件）
- 操作流畅，无卡顿

## 七、备注

- 序号持久化：使用 JSON 或文本文件保存当前序号
- 错误处理：截图失败时的友好提示
- 多显示器支持：如需支持，需额外处理
- 有在anconda虚拟环境JT_conda,python版本3.12.*，下载依赖库在JT_conda虚拟环境

## 七、更新功能
- 调用截图功能是，显示坐标和颜色值的背景要改为透明
- bug：现在调用截图功能右键只有复制坐标和颜色值，没有取消操作，请复制并取消截图
- 取消程序界面左上角图标和标题