# ✂️ 快捷截图工具

一个轻量级的 Windows 平台快捷截图工具，支持区域截图、放大镜、取色等功能。

![版本](https://img.shields.io/badge/版本-v1.0-blue.svg)
![平台](https://img.shields.io/badge/平台-Windows-lightgrey.svg)

## 功能特性

- 🎯 **区域截图** - 支持鼠标框选截图区域
- 🖼️ **自动保存** - 可选择自动保存截图到本地
- 📋 **一键复制** - 截图后自动复制到系统剪切板
- 🔍 **放大镜功能** - 鼠标悬停显示局部放大预览
- 🎨 **取色功能** - 点击任意位置获取像素颜色值
- 🖱️ **窗口拖动** - 主窗口可任意拖动位置
- 📐 **高分屏适配** - 解决 Windows 高 DPI 缩放导致的显示问题
- ⚡ **轻量级** - 单个 EXE 文件，无需安装

## 快速开始

### 安装依赖

```bash
pip install -r requirements.txt
```

### 运行程序

```bash
python main.py
```

### 打包为 EXE

```bash
python build.py
```

打包后的可执行文件位于 `dist/ScreenshotTool.exe`

## 项目结构

```
screenshot_tool/
├── main.py                 # 程序入口
├── screenshot_app.py       # 主界面应用类
├── capture_overlay.py      # 截图覆盖层（放大镜、取色等）
├── utils.py                # 工具函数（保存、剪贴板操作等）
├── build.py                # PyInstaller 打包脚本
├── requirements.txt        # 依赖列表
├── 截图工具计划书.md       # 项目计划文档
├── JT.ico                  # 程序图标
├── JT.png                  # 图标预览
└── dist/                   # 打包输出目录
    └── ScreenshotTool.exe  # 打包后的可执行文件
```

## 使用说明

### 截图操作

1. **截图**：点击界面上的"点击截图"按钮
2. **框选区域**：在屏幕上按住鼠标左键拖动选择区域
3. **完成截图**：松开鼠标左键，截图自动保存到剪切板

### 取色功能

- **鼠标悬停**：显示局部放大镜，可查看像素细节
- **点击取色**：点击任意位置获取当前像素的 RGB 颜色值
- **右键操作**：右键点击可复制当前坐标和色值信息

### 其他功能

- **自动保存**：勾选"自动保存"复选框，截图自动保存到程序目录
- **拖动窗口**：按住主窗口任意位置可拖动窗口位置

## 技术实现

### 核心依赖

| 库 | 用途 | 版本要求 |
|---|---|---|
| tkinter | GUI 界面 | Python 内置 |
| Pillow | 图像处理 | >=10.0.0 |
| mss | 屏幕捕获 | >=9.0.1 |
| pywin32 | Windows API 操作 | >=306 |
| pyperclip | 剪切板操作 | >=1.8.0 |
| PyInstaller | 打包工具 | >=6.0.0 |

### 关键特性实现

#### 高分屏适配

通过 `ctypes` 调用 Windows DPI 感知 API，确保在高 DPI 显示器上文字和界面不被裁剪：

```python
ctypes.windll.shcore.SetProcessDpiAwareness(2)
```

#### 放大镜功能

使用 `mss` 库快速捕获屏幕区域，并使用 `Pillow` 进行图像放大和显示：

```python
with mss.mss() as sct:
    monitor = {"left": x, "top": y, "width": width, "height": height}
    screenshot = sct.grab(monitor)
    image = Image.frombytes("RGB", screenshot.size, screenshot.bgra, "raw", "BGRX")
```

#### 剪切板操作

优先使用 Windows 原生 API (`win32clipboard`)，失败时回退到 PowerShell 方案：

```python
win32clipboard.OpenClipboard()
win32clipboard.SetClipboardData(win32con.CF_DIB, data)
win32clipboard.CloseClipboard()
```

## 打包说明

### 构建单个 EXE

项目使用 PyInstaller 打包，可生成单文件可执行程序。

**打包命令**：
```bash
python build.py
```

**输出文件**：`dist/ScreenshotTool.exe`

**注意事项**：
- 打包后的程序需要依赖 Python 解释器，体积较大（约 20MB）
- 首次运行时可能需要 Windows Defender 允许运行
- 图片保存在 EXE 所在目录，路径会自动适配

### 自定义打包

如需自定义打包配置，可修改 `build.py`：

```python
# 在 build() 函数中修改命令
cmd = [
    sys.executable,
    "-m",
    "PyInstaller",
    "--onefile",              # 单文件
    "--noconsole",            # GUI 模式
    "--name", "ScreenshotTool",  # 程序名
    "--icon", "JT.ico",        # 程序图标
    "main.py"                 # 入口文件
]
```

## 常见问题

### Q: 截图后为什么无法粘贴？

**A:** 检查系统剪贴板权限，或尝试使用 Ctrl+V 手动粘贴。

### Q: 放大镜显示空白？

**A:** 确保 mss 库已正确安装：`pip install mss`

### Q: 高分屏下文字模糊？

**A:** 程序已内置 DPI 适配，但某些旧版本 Windows 可能需要手动设置缩放比例。

### Q: 如何修改截图保存路径？

**A:** 当前版本保存路径固定为 EXE 所在目录。如需修改，请修改 `utils.py` 中的 `get_current_directory()` 函数。

## 开发计划

- [ ] 添加截图计时器功能
- [ ] 支持自定义截图快捷键
- [ ] 添加 PNG/WebP 等多种格式支持
- [ ] 支持图片编辑功能（标注、裁剪）
- [ ] 添加云端同步功能

## 贡献指南

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License

## 更新日志

### v1.0 (2026-02-26)
- ✨ 首次发布
- 🎯 实现区域截图功能
- 🖼️ 添加放大镜和取色功能
- 📋 实现自动保存和剪贴板复制
- 🖱️ 支持窗口拖动
- 📐 完善高分屏适配

## 致谢

感谢以下开源项目的支持：
- [mss](https://github.com/BoboTiG/python-mss) - 屏幕捕获
- [Pillow](https://pillow.readthedocs.io/) - 图像处理
- [PyInstaller](https://www.pyinstaller.org/) - 打包工具

---

**提示**：本工具仅用于学习和个人使用，请勿用于商业用途。
